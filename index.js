/**
 * @format
 */

import { AppRegistry } from "react-native";
import { name as appName } from "./app.json";
import { MentionsApp } from "./mentions-app.component";

AppRegistry.registerComponent(appName, () => MentionsApp);
